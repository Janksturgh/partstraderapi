/**
 * @description the normalizeToLowerCase function normalizes a string to lowercase which can be used
 * to ensure consistent case handling in string comparisons.
 * @param {string} string - The string to normalize.
 * @returns {string} The normalized string in lowercase.
 */

export default function normalizeToLowerCase(string) {
  return string.toLowerCase();
}
